function tabuada() {
    var numero = document.querySelector('input#number')
    var lista = document.querySelector('select#select')
    var tabu = number(numero.value)
    lista.appendChild()
    for (var neto = 0; neto <= 10; neto++) {
        var options = document.createElement(option)
        options.innerHTML = `lol`
        
    }
}